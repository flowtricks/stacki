# ripwire field notes v1 — one per line: <canonical-id or path> <TAB> <ISO-date> <TAB> <text> [<TAB> <HEAD sha> <TAB> <branch>]. Kept SORTED (merge-friendly union); dates are git committer-clock, not wall time; the trailing sha/branch pair is present only on provenance-stamped notes.
electron/astroParser.js	2026-09-15	convert in its OWN focused session (3,158 lines, the flagship, repo-wide hotspot #2). Delete the astroParser.d.ts seed contract on landing, then tighten PageModel.frontmatterLayout.slots from unknown to ImportSlot[]. parsePropSchema (ccx 334) is the hotspot inside it — convert types first, split control flow in a follow-up commit, never both in one commit.	0b64e550ba127ca78388fc0e74c97f54978c5c7f	ts-contracts/phase-0-gate
electron/preload.ts	2026-09-15	tsc emits 4-space indent and expands inline {stmt} blocks, so text-slicing test regexes must stay whitespace-tolerant (bridge, canvas-click, comment-markers, gap-bands, opened-class were all broken once). The frame-side DOM code runs in jsdom suites — narrow DOM types with predicates, not instanceof, across realms.	0b64e550ba127ca78388fc0e74c97f54978c5c7f	ts-contracts/phase-0-gate
electron/scratch2-7.js	2026-09-15	stray tsc-emitted outputs from the cssVars conversion experiments, still TRACKED in git (~200 lines total). Nothing requires them; delete in a standalone cleanup commit.	0b64e550ba127ca78388fc0e74c97f54978c5c7f	ts-contracts/phase-0-gate
scripts/afterPack.ts	2026-09-18	tsc TS2307 'Cannot find module app-builder-lib' — the module IS installed but extraneous (declared in no package.json). This fails build:scripts, which blocks every npm test run. Fix is declaring it a devDependency at the locked version, and nothing else.	7053ccfaff14a16df07ec7cf375ab6edaee12f21	refactor/architecture-consolidation
shared/scan.ts	2026-09-15	Wire shape is the truth, not the contract's first guess. schema travels as an ARRAY of PropField (main.js safeSchema -> App.jsx schemaFor); renderTag is {tag, prop} | null from rootTag; hasRest is parsed. The Map-based parsePropSchema is for in-process consumers only — IPC serialization collapses Maps to plain objects, so a Map-typed wire contract rejects every real payload (found 2026-09-15, first live end-to-end open; fixed b94457c).	0b64e550ba127ca78388fc0e74c97f54978c5c7f	ts-contracts/phase-0-gate
shared/tsconfig.json	2026-09-18	emits to ../dist/shared; shared/dist/ is a stale gitignored leftover from an older config and fails test/contracts/build-layout.test.ts ('generated output' in an authored tree). Delete shared/dist in the editor-core pre-flight — tracked in docs/editor-core-tracker.md verification record.	7053ccfaff14a16df07ec7cf375ab6edaee12f21	refactor/architecture-consolidation
